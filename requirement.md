这是一个非常棒的需求思路！要把它转化为一个给AI编程工具的提示词，我们需要增加**结构、清晰的步骤、技术细节和错误处理**，让AI能够理解并执行这个复杂的任务。

下面是一个经过丰富和优化的提示词，它将整个流程分解得非常清晰。

---

## 🤖 AI 编程工具提示词：全自动夸克网盘资源查找器

### 🎯 任务目标

创建一个全自动化的 Python 脚本（或类似编程语言/框架），用于批量查找用户豆瓣“想看”列表中的电影/剧集资源，并自动收集和操作找到的**夸克网盘分享链接**。

### 💻 技术栈要求

* **主语言:** Python (推荐使用 `requests`, `BeautifulSoup` 或 `Selenium` 等库)。
* **网络请求/爬虫:**
    * **豆瓣解析:** 使用 `requests` 获取 HTML，使用 `BeautifulSoup` 或 XPath 解析 DOM。
    * **Google 搜索:** 建议使用 `requests` 模拟搜索请求或使用 Google Custom Search API（如果需要更稳定和大量的搜索）。
* **浏览器自动化:** 必须使用 `Selenium` 或类似的工具 (`Playwright`) 来实现打开多个 Tab 和点击“加入收藏”的操作。
* **正则表达式/字符串处理:** 用于从网页内容中精准提取夸克网盘分享 URL。

### 📋 详细执行步骤（分阶段）

#### 阶段一：获取和解析豆瓣“想看”列表

1.  **输入:** 接受一个**豆瓣用户“想看”列表的 URL**（例如：`https://www.douban.com/people/xxx/wish`）。
2.  **数据获取:** 访问该 URL，并处理可能的翻页逻辑，获取列表中所有电影/剧集的**完整名称**。
3.  **输出:** 生成一个包含所有电影/剧集名称的列表，例如 `['电影A', '剧集B', '电影C']`。

#### 阶段二：自动化 Google 搜索和 URL 提取

1.  **循环搜索:** 遍历阶段一中生成的电影名称列表。
2.  **构造搜索关键词:** 对于每个电影名称，构造搜索词为：`"{电影名称}" + " 夸克"` (注意使用英文双引号确保电影名称被精确搜索)。
3.  **执行 Google 搜索:** 执行搜索，并抓取**前 10 个搜索结果的 URL**。
4.  **页面访问与筛选:**
    * 遍历这 10 个 URL。
    * 使用 `requests` 或 `Selenium` (headless 模式) 访问每个页面。
    * **核心提取逻辑:** 在每个页面的 HTML 源码中，使用**正则表达式**或关键词匹配，查找符合**夸克网盘分享链接格式**的 URL。
        * **通用格式参考:** `https://pan.quark.cn/s/` 或 `quark.cn/s/`。
    * **有效性检查:** 确保提取到的链接是完整的分享链接。
5.  **存储结果:** 将找到的 **有效的夸克网盘 URL** 与对应的电影名称一起存储在一个列表中，例如：`[{'title': '电影A', 'quark_url': '...'}]`。

#### 阶段三：浏览器自动化操作（Selenium）

1.  **启动浏览器:** 启动一个 `Selenium` 实例（如 Chrome/Edge，非 headless 模式）。
2.  **批量打开 Tab:** 遍历阶段二中收集到的所有夸克网盘 URL。
    * 使用 `driver.execute_script('window.open("URL", "_blank");')` 的方式在**新 Tab 中**打开每一个夸克链接。
3.  **自动收藏:**
    * 切换到每个新打开的 Tab。
    * **操作:** 找到夸克网盘分享页面上的 **“加入收藏”按钮**（或类似的“转存”、“保存到网盘”按钮）。
    * **点击:** 使用 `Selenium` 的定位方法（如 `xpath`, `CSS Selector`）执行**点击操作**。
    * **注意:** 需要考虑网盘页面可能存在的**登录验证**。脚本应在开始时提示用户手动登录，或在检测到未登录时暂停等待用户操作。
4.  **完成提示:** 所有操作完成后，打印一条成功的消息。

### 🛑 错误和异常处理

* **网络错误:** 处理 `requests.exceptions.RequestException` (超时、连接失败等)。
* **豆瓣解析失败:** 如果豆瓣页面结构变化，应能捕获 `IndexError` 或 `AttributeError`。
* **Google 反爬:** 如果搜索结果页无法加载，应有适当的延迟（`time.sleep()`）和重试机制。
* **夸克链接未找到:** 如果前 10 个搜索结果都没有夸克链接，应记录该电影为“未找到资源”并继续下一个。
* **Selenium 元素定位失败:** 针对“加入收藏”按钮的定位，应使用 `try...except` 块，如果找不到元素则跳过或记录错误。
* **登录状态:** 在 Selenium 阶段，应在尝试收藏前检查是否已登录。

### 💡 附加功能建议（可选）

1.  **进度条:** 在终端显示当前正在搜索第 $N$ 个电影（总共 $M$ 个）的进度条。
2.  **日志记录:** 将所有操作、成功结果、未找到资源的电影、失败的 URL 等信息记录到一个 `.log` 或 `.txt` 文件中。
3.  **配置化:** 将豆瓣 URL、搜索关键词后缀（如“夸克”）、`Selenium` 浏览器驱动路径等配置项作为程序参数或配置文件。